schema_version = "1.0.0"

id = "exact_radius"
version = "1.9.1"
name = "Exact Radius"
tagline = "Make selected vertex rings exact-radius circles, many at once"
maintainer = "Patrick Tiefenbacher"
type = "add-on"

blender_version_min = "4.2.0"

license = [
  "SPDX:GPL-3.0-or-later",
]

copyright = [
  "2026 Patrick Tiefenbacher",
]

tags = ["Mesh", "Modeling"]

# website: intentionally omitted — the source is hosted on a private Forgejo,
# not a public URL. Set this only if a publicly reachable mirror is ever made.

[build]
paths_exclude_pattern = [
  "__pycache__/",
  "/.git/",
  "/*.zip",
  "*.blend1",
  "/docs/",
  "/tests/",
  "/.gitignore",
]
